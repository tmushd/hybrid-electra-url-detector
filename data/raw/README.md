# Raw dataset

This repo includes `malicious_phish.csv.zip` for convenience.

It contains real-world URLs that may be malicious. Do not click them.

Expected CSV inside the zip: `malicious_phish.csv` with columns:
- `url`
- `type` ∈ {`benign`, `defacement`, `phishing`, `malware`}
