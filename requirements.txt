numpy>=1.24,<3
pandas>=2.0,<3
scikit-learn>=1.3,<2
joblib>=1.3,<2
tldextract>=5.1,<6

# NOTE: torch installation differs across OS/CPU/GPU. See README if this fails.
torch>=2.1,<3

# Allow both Transformers v4 and v5; code adapts to API differences.
transformers>=4.42,<6
accelerate>=0.30,<2
